// Tipos de dados primitivos - string, number, undefined, null, boolean, symbol

// vejamos os tipos das variaveis abaixo:

let nome = "Robson" // string
let idade = 15; // number
let valor; // undefined -> nao aponta para nenhum lugar na memoria
let impossivel = null; // nulo -> nao aponta para nenhum lugar na memoria
const valorBoolean = true; // valor booleano -> true ou false


// qual a diferenca entre uma variavel nula e undefined?
// a variavel nula significa que o programador quis que ela fosse assim
// já undefined, significa que o proprio JS definiu a variavel com undefined

// para saber o tipo de uma variavel utilize o tpyeof.

// atividade
// Digite as variaveis abaixo, sendo que:
// varA deverá receber o valor B
// varB deverá receber o valor C
// varC deverá receber o valor A

// obs: voce não pode atribuir um novo valor para as variaveis abaixo, utilize somente variavel.
// Exemplo do que não fazer: varA = "B".

let varA = 'A'; // B
let varB = 'B'; // C
let varC = 'C'; // A
